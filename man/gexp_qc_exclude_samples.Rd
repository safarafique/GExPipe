% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_qc_pipeline.R
\name{gexp_qc_exclude_samples}
\alias{gexp_qc_exclude_samples}
\title{Exclude selected samples from download/QC state lists}
\usage{
gexp_qc_exclude_samples(
  combined_expr_raw,
  micro_expr_list,
  rna_counts_list,
  unified_metadata = NULL,
  samples_to_exclude
)
}
\arguments{
\item{combined_expr_raw}{Matrix genes x samples.}

\item{micro_expr_list}{Named list of microarray matrices.}

\item{rna_counts_list}{Named list of RNA-seq matrices.}

\item{unified_metadata}{Optional metadata data.frame with SampleID column.}

\item{samples_to_exclude}{Character vector of sample IDs.}
}
\value{
List containing updated matrices/lists/metadata.
}
\description{
Exclude selected samples from download/QC state lists
}
\examples{
expr <- matrix(rnorm(300), nrow = 30, ncol = 10)
rownames(expr) <- paste0("Gene", seq_len(nrow(expr)))
colnames(expr) <- paste0("S", seq_len(ncol(expr)))
meta <- data.frame(SampleID = colnames(expr), stringsAsFactors = FALSE)
res <- gexp_qc_exclude_samples(
  combined_expr_raw = expr,
  micro_expr_list = list(D1 = expr[, 1:5, drop = FALSE]),
  rna_counts_list = list(D2 = expr[, 6:10, drop = FALSE]),
  unified_metadata = meta,
  samples_to_exclude = c("S1", "S10")
)
ncol(res$combined_expr_raw)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_qc_pipeline.R
\name{gexp_qc_detect_outliers}
\alias{gexp_qc_detect_outliers}
\title{Detect sample outliers from expression matrix}
\usage{
gexp_qc_detect_outliers(expr, top_n = 5000L)
}
\arguments{
\item{expr}{Numeric matrix with genes in rows and samples in columns.}

\item{top_n}{Integer number of top variable genes to use (default 5000).}
}
\value{
List with outlier detection results.
}
\description{
Uses two methods on the top variable genes:
\itemize{
\item PCA Mahalanobis distance (97.5\% chi-square cutoff),
\item Sample connectivity in signed network (power = 6; mean - 2*SD threshold).
}
}
\examples{
expr <- matrix(rnorm(400), nrow = 40, ncol = 10)
rownames(expr) <- paste0("Gene", seq_len(nrow(expr)))
colnames(expr) <- paste0("S", seq_len(ncol(expr)))
out <- gexp_qc_detect_outliers(expr, top_n = 20)
length(out$all_outliers)
}

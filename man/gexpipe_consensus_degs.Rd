% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_consensus_pipeline.R
\name{gexpipe_consensus_degs}
\alias{gexpipe_consensus_degs}
\title{Consensus DEGs from separate RNA-seq and microarray DE}
\usage{
gexpipe_consensus_degs(rna_sig, micro_sig, require_same_direction = TRUE)
}
\arguments{
\item{rna_sig}{Significant-gene table from RNA-seq DE (\code{Gene}, \code{logFC},
\code{adj.P.Val}, optional \code{Significance}).}

\item{micro_sig}{Significant-gene table from microarray DE (same columns).}

\item{require_same_direction}{logical; if TRUE, drop genes with opposite
fold-change signs.}
}
\value{
list with \code{table}, \code{genes}, \code{n_rna}, \code{n_micro}, \code{n_overlap},
\code{n_consensus}, \code{n_discordant}, \code{rna_only}, \code{micro_only}.
}
\description{
Keeps genes significant on \strong{both} platforms. When
\code{require_same_direction} is TRUE (default), the logFC sign must match.
}
\details{
Downstream tables use mean logFC and the more conservative (larger)
adjusted p-value so WGCNA / common-gene steps can reuse \code{sig_genes}.
}
\examples{
rna <- data.frame(
  Gene = c("A", "B", "C"),
  logFC = c(1.2, -0.8, 1.1),
  adj.P.Val = c(0.01, 0.02, 0.03),
  Significance = c("Up-regulated", "Down-regulated", "Up-regulated"),
  stringsAsFactors = FALSE
)
micro <- data.frame(
  Gene = c("A", "B", "D"),
  logFC = c(0.9, -1.0, 1.4),
  adj.P.Val = c(0.02, 0.01, 0.04),
  Significance = c("Up-regulated", "Down-regulated", "Up-regulated"),
  stringsAsFactors = FALSE
)
out <- gexpipe_consensus_degs(rna, micro)
out$genes
}

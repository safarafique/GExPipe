% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_wgcna_pipeline.R
\name{gexp_wgcna_prepare}
\alias{gexp_wgcna_prepare}
\title{Prepare expression and sample data for WGCNA}
\usage{
gexp_wgcna_prepare(
  expr,
  metadata,
  gene_mode = c("top_variable", "all_common"),
  top_genes = 5000L,
  min_samples_frac = 0.5
)
}
\arguments{
\item{expr}{Matrix of expression values (genes x samples), typically
batch-corrected.}

\item{metadata}{Data.frame with sample metadata; should contain
either rownames matching \code{colnames(expr)} or a \code{SampleID} column.}

\item{gene_mode}{"all_common" or "top_variable" (default).}

\item{top_genes}{If \code{gene_mode = "top_variable"}, the number of
most variable genes to select (default 5000).}

\item{min_samples_frac}{Minimum fraction of samples with non-missing
expression required for a gene to be kept (default 0.5).}
}
\value{
A list with elements:
\item{datExpr}{numeric matrix (samples x genes) for WGCNA}
\item{sample_info}{data.frame with sample metadata aligned to datExpr}
\item{gene_variance_table}{data.frame with gene variance and rank}
}
\description{
This helper mirrors the "Prepare WGCNA Data" step:
\itemize{
\item uses batch-corrected expression (or combined expression if needed),
\item drops samples/genes with too many missing values,
\item optionally filters to top variable genes,
\item enforces a minimum fraction of non-missing samples per gene,
\item returns a WGCNA-ready \code{datExpr} matrix (samples x genes) and
companion sample info and gene-variance table.
}
}
\examples{
expr <- matrix(rnorm(600), nrow = 60)
rownames(expr) <- paste0("Gene", seq_len(nrow(expr)))
colnames(expr) <- paste0("S", seq_len(ncol(expr)))
metadata <- data.frame(
  SampleID = colnames(expr),
  Condition = rep(c("Normal", "Disease"), each = 5),
  Dataset = rep(c("D1", "D2"), each = 5),
  stringsAsFactors = FALSE
)
prep <- gexp_wgcna_prepare(expr, metadata, gene_mode = "top_variable", top_genes = 50)
dim(prep$datExpr)
}

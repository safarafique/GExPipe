% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_qc_pipeline.R
\name{gexp_qc_prepare_boxplot_data}
\alias{gexp_qc_prepare_boxplot_data}
\title{Prepare QC boxplot data from combined expression}
\usage{
gexp_qc_prepare_boxplot_data(
  combined_expr_raw,
  micro_expr_list,
  rna_counts_list,
  max_points = 500000L
)
}
\arguments{
\item{combined_expr_raw}{Matrix genes x samples.}

\item{micro_expr_list}{Named list of microarray matrices.}

\item{rna_counts_list}{Named list of RNA-seq matrices.}

\item{max_points}{Maximum rows to keep for plotting.}
}
\value{
data.frame with Expression, Sample, Platform.
}
\description{
Prepare QC boxplot data from combined expression
}
\examples{
expr <- matrix(rnorm(120), nrow = 12, ncol = 10)
rownames(expr) <- paste0("Gene", seq_len(nrow(expr)))
colnames(expr) <- paste0("S", seq_len(ncol(expr)))
df <- gexp_qc_prepare_boxplot_data(
  combined_expr_raw = expr,
  micro_expr_list = list(D1 = expr[, 1:5, drop = FALSE]),
  rna_counts_list = list(D2 = expr[, 6:10, drop = FALSE]),
  max_points = 1000
)
head(df)
}

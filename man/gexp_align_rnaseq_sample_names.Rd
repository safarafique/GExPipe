% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_download_pipeline.R
\name{gexp_align_rnaseq_sample_names}
\alias{gexp_align_rnaseq_sample_names}
\title{Align RNA-seq count-matrix column names with GEO sample metadata}
\usage{
gexp_align_rnaseq_sample_names(count_matrix, metadata = NULL, gse_id = NULL)
}
\arguments{
\item{count_matrix}{Numeric matrix (genes x samples).}

\item{metadata}{Optional GEO pData with sample IDs as row names.}

\item{gse_id}{GEO series accession (used for fallback naming).}
}
\value{
Matrix with improved column names.
}
\description{
GExPipe users enter \strong{GSE accessions} in the app; this helper runs
automatically during GEO download when a count file has no sample headers
(e.g. \code{data.table::fread} assigns \code{V2}, \code{V3}, ...). Columns are renamed
to GSM IDs from GEO pData so QC, normalization, and group selection see
individual samples (fixes issues such as a single \strong{V2} bar in outlier QC).
}
\examples{
# GSE137136-style: headerless columns after download, GSM IDs in metadata.
mat <- matrix(1:4, nrow = 2, ncol = 2,
              dimnames = list(c("GENE1", "GENE2"), c("V2", "V3")))
meta <- data.frame(title = c("sample 1", "sample 2"),
                   row.names = c("GSM111", "GSM222"))
out <- gexp_align_rnaseq_sample_names(mat, meta, "GSE137136")
colnames(out)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_download_pipeline.R
\name{gexp_download_finalize_common_genes}
\alias{gexp_download_finalize_common_genes}
\title{Finalize common genes and combined matrix after download/mapping}
\usage{
gexp_download_finalize_common_genes(
  micro_expr_list,
  rna_counts_list,
  all_genes_list
)
}
\arguments{
\item{micro_expr_list}{Named list of microarray matrices (genes x samples).}

\item{rna_counts_list}{Named list of RNA-seq matrices (genes x samples).}

\item{all_genes_list}{Named list of rowname vectors per dataset.}
}
\value{
List with updated lists, common_genes, combined_expr_raw, and status.
}
\description{
Finalize common genes and combined matrix after download/mapping
}
\examples{
m1 <- matrix(1:12, nrow = 3, dimnames = list(c("A", "B", "C"), paste0("S", 1:4)))
m2 <- matrix(1:12, nrow = 3, dimnames = list(c("B", "C", "D"), paste0("T", 1:4)))
out <- gexp_download_finalize_common_genes(
  micro_expr_list = list(D1 = m1),
  rna_counts_list = list(D2 = m2),
  all_genes_list = list(D1 = rownames(m1), D2 = rownames(m2))
)
out$common_genes
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gexp_de_pipeline.R
\name{gexpipe_run_limma_on_subset}
\alias{gexpipe_run_limma_on_subset}
\title{Run limma DE on one platform (or one study) subset}
\usage{
gexpipe_run_limma_on_subset(
  expr,
  metadata,
  logfc_cutoff = 0.5,
  padj_cutoff = 0.05,
  ref_lab = "Normal",
  alt_lab = "Disease"
)
}
\arguments{
\item{expr}{Numeric matrix (genes x samples).}

\item{metadata}{Sample metadata with \code{Condition} and optional \code{Dataset}.}

\item{logfc_cutoff}{Numeric log2 fold-change cutoff.}

\item{padj_cutoff}{Numeric adjusted P-value cutoff.}

\item{ref_lab}{Reference condition label (default \code{Normal}).}

\item{alt_lab}{Alternate condition label (default \code{Disease}).}
}
\value{
list with \code{de_results}, \code{sig_genes}, \code{filter_note}, \code{sample_info},
\code{formula_desc}.
}
\description{
Uses per-platform normalized expression (not the cross-platform
batch-corrected matrix). Single-dataset subsets use a Condition contrast;
multiple datasets on the same platform use \code{\link[=gexpipe_build_de_design]{gexpipe_build_de_design()}}.
}
\examples{
expr <- matrix(rnorm(200), nrow = 20)
rownames(expr) <- paste0("G", seq_len(nrow(expr)))
colnames(expr) <- paste0("S", seq_len(ncol(expr)))
meta <- data.frame(
  Condition = rep(c("Normal", "Disease"), each = 5),
  row.names = colnames(expr),
  stringsAsFactors = FALSE
)
gexpipe_run_limma_on_subset(expr, meta)
}
